{% extends '@!EasyAdmin/page/content.html.twig' %}
{% block main %}
	<div class="container">
		<div class="row">
			<div class="col-md-12">
				<div class="card">
					<div class="card-body">
						{{form_start(form)}}
						<input type="submit" class="btn btn-primary" value="{{ 'prolyfix.procurement.doc_parser.submit'|trans({}, 'messages') }}">
						{{form_end(form)}}
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
